package domain

// {{ .Model.Name }} is generated by StackSprint from the schema builder.
type {{ .Model.Name }} struct {
	ID int `json:"id" gorm:"primaryKey;column:id"`
{{- range .Model.Fields }}
	{{ .Name }} {{ .Type }} `json:"{{ .JSONName }}" gorm:"column:{{ .JSONName }}"`
{{- end }}
}